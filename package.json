{
  "name": "shiye-stationery",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/cart.test.js && npm --prefix backend test",
    "test:frontend": "node --test tests/cart.test.js",
    "test:backend": "npm --prefix backend test",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "vue": "^3.5.0",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0",
    "@vitejs/plugin-vue": "^6.0.0",
    "vite": "^7.3.0"
  }
}
