{
  "name": "shiye-stationery-backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test",
    "db:setup": "node --env-file=.env scripts/setup-database.js",
    "test:db": "node --env-file=.env --test integration/database.js integration/orders.js"
  },
  "dependencies": {
    "argon2": "^0.45.1",
    "cookie": "^2.0.1",
    "express": "^5.2.1",
    "mysql2": "^3.24.4"
  }
}
